@page "/region/new"
@page "/region/{Id:int}/edit"
@using Microsoft.AspNetCore.Authorization
@namespace Jhipster.Client.Pages.Entities.Region

@attribute [Authorize(Roles = "ROLE_USER")]

<div class="row justify-content-center">
    <div class="col-8">
        <EditForm Model="@RegionModel" OnValidSubmit="@Save">
            <h2 id="jhi-region-heading">Create or edit a Region</h2>
            <div>
                @*
                    TODO ADD ERROR
                    <jhi-alert-error></jhi-alert-error>
                *@

                @if (Id != 0)
                {
                    <div class="form-group">
                        <label for="id">ID</label>
                        <input type="text" class="form-control" id="id" name="id" readonly @bind-value="@RegionModel.Id" />
                    </div>
                }

                <div class="form-group">
                    <label class="form-control-label" for="field_regionName">Region Name</label>
                    <input type="text" class="form-control" name="regionName" id="field_regionName"
                            @bind-value="@RegionModel.RegionName" />
                </div>

            </div>

            <div>
                <button type="button" id="cancel-save" class="btn btn-secondary" @onclick="Back">
                    <Icon Name='"fa-ban"' />&nbsp;<span>Cancel</span>
                </button>

                <button type="submit" id="save-entity" class="btn btn-primary">
                    <Icon Name='"fa-save"' />&nbsp;<span>Save</span>
                </button>
            </div>
        </EditForm>
    </div>
</div>
